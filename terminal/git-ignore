# Global ignores — applied to every repository without touching any repo's own
# .gitignore. Git reads this path automatically when core.excludesFile is unset.

# direnv: .envrc lives inside a project and can hold credentials.
.envrc
.direnv/
